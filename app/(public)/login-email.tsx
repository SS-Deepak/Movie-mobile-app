import LoginEmailScreen from '@/screens/login-screen/email'
import React from 'react'

const LoginEmail = () => {
  return (
    <LoginEmailScreen onSuccess={() => { }} />
  )
}

export default LoginEmail