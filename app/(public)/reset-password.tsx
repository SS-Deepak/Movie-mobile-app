import ResetPasswordScreen from '@/screens/register-screen/reset-password'
import React from 'react'

const ResetPassword = () => {
  return (
    <ResetPasswordScreen onSubmit={() => { }} />
  )
}

export default ResetPassword