import LoginPasswordScreen from '@/screens/login-screen/password'
import React from 'react'

const LoginPassword = () => {
  return (
    <LoginPasswordScreen onSubmit={()=>{}}/>
  )
}

export default LoginPassword